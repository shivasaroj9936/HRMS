import { AvoidFirstSpaceDirective } from './avoid-first-space.directive';

describe('AvoidFirstSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new AvoidFirstSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
