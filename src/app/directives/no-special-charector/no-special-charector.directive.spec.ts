import { NoSpecialCharectorDirective } from './no-special-charector.directive';

describe('NoSpecialCharectorDirective', () => {
  it('should create an instance', () => {
    const directive = new NoSpecialCharectorDirective();
    expect(directive).toBeTruthy();
  });
});
